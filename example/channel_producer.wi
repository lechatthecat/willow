async fn producer(ch: Channel<i64>) {
    ch.send(10);
    ch.send(20);
    ch.send(30);
    ch.close();
}

async fn main() {
    let ch = Channel<i64>::new();
    let h = producer(ch);

    println(ch.recv());
    println(ch.recv());
    println(ch.recv());

    await h;
}
