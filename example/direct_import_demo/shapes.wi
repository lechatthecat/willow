// A module exporting a public class and enum, used via direct type imports.
module shapes;

pub class Point {
    pub x: i64;
    pub y: i64;
    pub fn sum(self) -> i64 { return self.x + self.y; }
}

pub enum Kind {
    Small,
    Big(i64),
}
