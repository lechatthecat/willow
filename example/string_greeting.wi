// Strings: parameters, returns, concatenation, and equality.

fn greet(name: String) -> String {
    return "hello, " + name;
}

fn main() {
    let name = "willow";
    println(greet(name));
    println(name == "willow");
}
