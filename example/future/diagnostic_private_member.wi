// status: future
// feature: class visibility diagnostics
// expected: compile error, field `balance` is private

pub class Account {
    balance: i64;

    pub init(self, balance: i64) {
        self.balance = balance;
    }
}

fn main() {
    let account = new Account(500);
    println(account.balance);
}
